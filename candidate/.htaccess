<IfModule mod_rewrite.c>
# Enable mod_rewrite
RewriteEngine On

# Specify the folder in which the application resides.
# Use / if the application is in the root. 
RewriteBase /evotingSystem/candidate



</IfModule>
ErrorDocument 404 /evotingSystem/candidate/index.php?page=errorpage